services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: finmonitor
      POSTGRES_USER: finmonitor
      POSTGRES_PASSWORD: finmonitor
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U finmonitor -d finmonitor"]
      interval: 2s
      timeout: 3s
      retries: 10
    ports:
      - "5661:5432"

  api1:
    build:
      context: .
      dockerfile: backend/src/FinMonitor.Api/Dockerfile
    environment: &api-env
      Storage__Provider: PostgresEf
      Storage__ConnectionString: "Host=postgres;Port=5432;Database=finmonitor;Username=finmonitor;Password=finmonitor"
      Redis__Enabled: "true"
      Redis__ConnectionString: "redis:6379,abortConnect=false"
      Deployment__Mode: "Distributed"
    depends_on:
      redis:
        condition: service_started
      postgres:
        condition: service_healthy

  api2:
    build:
      context: .
      dockerfile: backend/src/FinMonitor.Api/Dockerfile
    environment: *api-env
    depends_on:
      redis:
        condition: service_started
      postgres:
        condition: service_healthy

  api3:
    build:
      context: .
      dockerfile: backend/src/FinMonitor.Api/Dockerfile
    environment: *api-env
    depends_on:
      redis:
        condition: service_started
      postgres:
        condition: service_healthy

  frontend-lb:
    build:
      context: .
      dockerfile: docker/nginx-lb.Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - api1
      - api2
      - api3

volumes:
  postgres-data:
